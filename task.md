Person 2: The Full-Stack & Infrastructure Lead
Core Focus: Application frontend, user session management, and database architecture. This role handles the "Chatbot UI" box and the bottom infrastructure row.

Task 1: UI Development 

Build the main web interface using HTML, CSS and JS.

Create the input fields fion (using a library like streamlit-authenticator).

Ensure that when a user logs in, their session state is isolated from other users.

Task 3: Conversation Management & Database

Set up the MongoDB database to store user credentials and chat histories.

Implement LangChain's memory modules (like SQLChatMessageHistory) so the chatbot can "remember" previous questions within the same video session.or the YouTube URL and the chat interface (chat history bubbles, text input box).

Task 2: Authentication & Security

Implement multi-user authenticat